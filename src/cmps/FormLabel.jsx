import React from 'react'
import FormLabel from '@mui/material/FormLabel';

const StyledFormLabel = ({text}) => <FormLabel sx={{fontSize: '1rem', fontWeight:'800', color: 'black'}}>{text}</FormLabel>

export default StyledFormLabel